# Web 框架
fastapi>=0.116
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 数据库 / ORM
SQLAlchemy>=2.0
PyMySQL>=1.1            # MySQL 驱动（切换 MySQL 时使用）
alembic>=1.13          # 数据库迁移（P6 收尾启用）

# 配置 / 校验
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# 鉴权（P2 启用）
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# 后台任务（P4 启用：模拟训练调度）
APScheduler>=3.10
