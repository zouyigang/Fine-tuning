# Web 框架
fastapi>=0.116
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 数据库 / ORM
SQLAlchemy>=2.0
PyMySQL>=1.1            # MySQL 驱动（切换 MySQL 时使用）
alembic>=1.13          # 数据库迁移（P6 收尾启用）

# 配置 / 校验
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# 鉴权（P2 启用）
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# 后台任务（P4 启用：模拟训练调度）
APScheduler>=3.10
pytz>=2024.1            # APScheduler 时区 + trainer.py 直接 import

# 文件导出（评估报告 PDF / Excel、错误案例导出）
reportlab>=4.0         # PDF 生成（内置 CJK 字体 STSong-Light）
openpyxl>=3.1          # Excel(xlsx) 生成

# 真实微调引擎（ENGINE_MODE=real）：调 LLaMA-Factory 跑训练
# 注意：torch / llamafactory 体量大且需匹配 CUDA，单独按 docs/真实微调引擎方案.md 安装，不在此 pin
# QLoRA(4-bit) 需 bitsandbytes，且其二进制必须含匹配当前 CUDA 的版本：
#   CUDA 13（torch cu130 / Blackwell sm_120）需 bitsandbytes>=0.49.2（含 libbitsandbytes_cuda130）；
#   旧版（≤0.46）无 cuda130 二进制 → QLoRA 必失败（引擎 qlora_supported 会提前拦截并提示）。LoRA 不依赖它。
PyYAML>=6.0            # 生成 LF 训练 YAML 配置
nvidia-ml-py>=12.0     # GPU 利用率/显存真实采样（pynvml）
